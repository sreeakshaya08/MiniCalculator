# MiniCalculator
Python Modular Calculator  A simple command-line calculator built using Python that demonstrates the concept of modules and functions. Instead of writing all the logic in a single file, each arithmetic operation is implemented in a separate module, making the project clean, organized, and easy to maintain.
